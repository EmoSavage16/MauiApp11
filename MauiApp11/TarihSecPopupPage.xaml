<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             x:Class="MauiApp11.TarihSecPopupPage"
             Title="">

    <StackLayout Padding="20" Spacing="15">
        <Label Text="Başlangıç Tarihi" />
        <DatePicker x:Name="BaslangicDatePicker" />

        <Label Text="Bitiş Tarihi" />
        <DatePicker x:Name="BitisDatePicker" />

        <Label x:Name="ToplamFiyatLabel" 
       Text="Toplam Fiyat: ₺0" 
       TextColor="White" 
       FontSize="18" 
       HorizontalOptions="Center" 
       Margin="0,10,0,0" />

        <Button Text="Onayla" Clicked="OnaylaButton_Clicked"/>
        <Button Text="İptal" Clicked="IptalButton_Clicked" />
    </StackLayout>
</ContentPage>